---
import "../styles/global.css";
---

<h1>Coming soon: the fast, friendly and powerful rescue-tool</h1>
<p class="subtitle">
    Liveboot will run entirely from memory so you can rescue, clone, and repair
    without touching disks until you want to. Comes with sane defaults and
    UI-first tools.
</p>

<style>
    h1 {
        font-size: clamp(2.4rem, 2vw + 2rem, 3.5rem);
        font-weight: 800;
        margin: 10px 0 12px;
    }

    .subtitle {
        font-size: clamp(1rem, 0.5vw + 1rem, 1.25rem);
        color: var(--ink-soft);
        max-width: 60ch;
        margin: 0 auto 28px;
    }
</style>
